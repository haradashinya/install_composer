from setuptools import setup
setup(name='install_composer',
      version='0.1',
      description='install composer',
      author='furodrive',
      license='MIT',
      url='http://github.com/storborg/funniest',
      packages=['install_composer'],
      zip_safe=False)
